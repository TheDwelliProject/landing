This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

> **Important:** Do not set `NODE_ENV=development` in your shell or CI environment before running `pnpm build`. Next.js sets `NODE_ENV=production` internally during builds, and overriding it causes cryptic build failures (e.g. `<Html> should not be imported outside of pages/_document`).
